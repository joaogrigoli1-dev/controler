# ============================================================
# Controler — Configurações
# ============================================================
# API key da Anthropic fica no banco SQLite (segurança).
# Aqui ficam apenas configurações não-sensíveis.

server:
  host: "127.0.0.1"
  port: 3001

myclinicsoft:
  local_path: "/Users/jhgm/Documents/DEV/myclinicsoft"
  github_branch: "main"

  # ── Coolify (srv1) ─────────────────────────────────────────
  # Deploy agora é via Coolify API — sem SSH/rsync/PM2.
  coolify_url: "http://62.72.63.18:8000"
  coolify_token: "2|PACNSa1HBN0AkS5LKsp4x5YeNS95QirqOYyAsLg30ef58ece"
  coolify_app_uuid: "jckc0ccwssowwc0oocw80ogs"
  coolify_buffer_uuid: "nw48cggkk4ss4g00s08s8wkw"
  coolify_server_uuid: "j4ws844wcg400kwsc0sswocg"
  server_ip: "62.72.63.18"
  app_port: 5000
  buffer_port: 3001

  # ── SSH (para comandos diretos no servidor quando necessário) ──
  ssh_key: "~/.ssh/coolify_server"
  server_user: "root"

controler:
  # ── Coolify deploy do próprio Controler ──────────────────────
  coolify_app_uuid: "hs8c0csogg4008o44k8w008g"
  domain: "controler.net.br"
  app_port: 3001
